spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/supply_chain_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres123}
  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD:}

logging:
  level:
    com.supplychain: INFO
